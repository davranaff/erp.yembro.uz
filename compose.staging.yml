services:
  api:
    networks:
      public:
        aliases:
          - staging-api

  frontend:
    networks:
      public:
        aliases:
          - staging-frontend

  catalog:
    networks:
      public:
        aliases:
          - staging-catalog

volumes:
  postgres_data:
    external: true
    name: ${POSTGRES_VOLUME_NAME:-postgres_data_staging}
  redis_data:
    external: true
    name: ${REDIS_VOLUME_NAME:-redis_data_staging}
  uploads_data:
    external: true
    name: ${UPLOADS_VOLUME_NAME:-uploads_staging}
  staticfiles_data:
    external: true
    name: ${STATICFILES_VOLUME_NAME:-staticfiles_staging}

networks:
  internal:
    external: true
    name: ${INTERNAL_NETWORK_NAME:-yembro_staging_internal}
  public:
    external: true
    name: ${PUBLIC_NETWORK_NAME:-yembro_staging_public}
