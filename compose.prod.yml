services:
  api:
    networks:
      public:
        aliases:
          - prod-api

  frontend:
    networks:
      public:
        aliases:
          - prod-frontend

volumes:
  postgres_data:
    external: true
    name: ${POSTGRES_VOLUME_NAME:-postgres_data_prod}
  redis_data:
    external: true
    name: ${REDIS_VOLUME_NAME:-redis_data_prod}
  uploads_data:
    external: true
    name: ${UPLOADS_VOLUME_NAME:-uploads_prod}

networks:
  internal:
    external: true
    name: ${INTERNAL_NETWORK_NAME:-yembro_prod_internal}
  public:
    external: true
    name: ${PUBLIC_NETWORK_NAME:-yembro_prod_public}
